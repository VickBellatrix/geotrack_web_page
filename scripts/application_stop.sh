#!/bin/bash

# Detener el servicio de la aplicación (por ejemplo, si estás ejecutando un servidor Node.js)
#sudo systemctl stop server.js
# O detener el proceso de la aplicación directamente (si no estás usando un servicio)
# sudo pkill -f "node app.js"

# Otros comandos de limpieza o preparación si es necesario
